[metadata]
name = Create branch

[options]
package_dir =
    = src
packages = find:
install_requires =
    click
    PyGithub

[options.entry_points]
console_scripts =
    create-branch = create_branch.cli:main

[options.packages.find]
where = src
